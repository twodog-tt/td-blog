Name: blog-api
Host: 127.0.0.1
Port: 8080

Auth:
  AccessSecret: "secret-key" # JWT 加密用的密钥，必须是一个不为空的字符串
  AccessExpire: 3600 # JWT 过期时间，单位是秒（比如 3600 表示 1 小时）

DB:
  DataSource: root:123456789@tcp(127.0.0.1:3306)/td-blog?charset=utf8mb4&parseTime=True&loc=Local